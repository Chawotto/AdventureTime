name: 🚀 Build and Deploy AdventureTime to Railway

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    container: ghcr.io/railwayapp/cli:latest # Используем Docker-контейнер с Railway CLI

    steps:
      # 1. Клонируем код
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      # 2. Устанавливаем Java 17
      - name: ☕ Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      # 3. Кэшируем зависимости Maven
      - name: 💾 Cache Maven packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # 4. Выдаём права на выполнение mvnw
      - name: 🔓 Grant execute permission for mvnw
        run: chmod +x mvnw

      # 5. Сборка проекта через Maven
      - name: 🛠️ Build with Maven
        run: ./mvnw clean package -DskipTests

      # 6. Деплоим на Railway
      - name: 🚀 Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_SERVICE_ID: ${{ secrets.RAILWAY_SERVICE_ID }}
        run: |
          echo "Service ID: $RAILWAY_SERVICE_ID"
          echo "Railway Token: $RAILWAY_TOKEN"
          railway up --service $RAILWAY_SERVICE_ID
