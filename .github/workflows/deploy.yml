name: 🚀 Build and Deploy AdventureTime to Railway

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Клонируем код
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      # 2. Устанавливаем Java 17
      - name: ☕ Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      # 3. Кэшируем зависимости Maven
      - name: 💾 Cache Maven packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # 4. Выдаём права на выполнение mvnw
      - name: 🔓 Grant execute permission for mvnw
        run: chmod +x mvnw

      # 5. Сборка проекта через Maven
      - name: 🛠️ Build with Maven
        run: ./mvnw clean package -DskipTests

      # 6. Устанавливаем Railway CLI
      - name: 📦 Install Railway CLI
        run: |
          curl -fsSL https://railway.app/install.sh | sh

      # 7. Логинимся и деплоим на Railway
      - name: 🚀 Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          echo "RAILWAY_TOKEN is set to: $RAILWAY_TOKEN"
          echo "RAILWAY_PROJECT_ID is set to: $RAILWAY_PROJECT_ID"
          railway version
          railway link $RAILWAY_PROJECT_ID
          railway up